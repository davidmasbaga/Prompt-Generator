["For the output use a table format", "For the output use a list format", "For the output use Dialogue/Conversational format", "For the output use a list of keywords in your content and bold them in the output"]